This folder collects smoke and demo namelists used for fast validation runs.

Run the commands from the repository root so relative input and output paths
inside the namelists resolve correctly.

Generated template namelists such as `mhm-para-template.nml`,
`mhm-output-template.nml`, and `mhm-template.nml` remain in the repository
root.
